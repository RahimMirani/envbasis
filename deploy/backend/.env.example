# Docker Compose deployment settings. Copy to .env and keep it private.
API_DOMAIN=api.example.com

# Generate with: openssl rand -hex 32
# This value is used only inside the private Docker network.
REDIS_PASSWORD=replace-with-a-long-random-value
