APP_ENV=development

# Must match the console backend's MACHINE_AUTH_JWT_SECRET and JWT settings.
MACHINE_AUTH_JWT_SECRET=replace-me
MACHINE_AUTH_JWT_ALGORITHM=HS256
MACHINE_AUTH_JWT_ISSUER=envbasis-machine-auth
MACHINE_AUTH_JWT_AUDIENCE=envbasis-machine

# Control-plane credential channel (preferred). When set, provider keys are
# fetched from EnvBasis instead of local env vars.
# CONTROL_PLANE_URL=http://127.0.0.1:8000
# PROXY_SERVICE_TOKEN=<same-as-backend-PROXY_SERVICE_TOKEN>
# CREDENTIAL_CACHE_TTL_SECONDS=30

# Local/test fallback provider credentials when CONTROL_PLANE_URL is unset.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GITHUB_TOKEN=

UPSTREAM_TIMEOUT_SECONDS=60
MAX_REQUEST_BYTES=2097152
MAX_RESPONSE_BYTES=20971520
